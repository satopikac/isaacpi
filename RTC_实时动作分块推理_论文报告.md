# Real-Time Chunking (RTC)：VLA 推理时实时执行技术 —— 论文详细报告

> 本报告聚焦 VLA（Vision-Language-Action）模型推理阶段的**实时性（Real-Time）**问题，核心论文为 Physical Intelligence 发表于 **NeurIPS 2025** 的：
>
> **Real-Time Execution of Action Chunking Flow Policies**
> Kevin Black, Manuel Y. Galliker, Sergey Levine（Physical Intelligence / UC Berkeley）
> arXiv: [2506.07339](https://arxiv.org/abs/2506.07339)（博客版标题为 *Real-Time Action Chunking with Large Models*，发布于 2025-06-09）
> 项目主页：https://pi.website/research/real_time_chunking
>
> 并附带其 **2025-12 训练时改进版后续论文**：
> **Training-Time Action Conditioning for Efficient Real-Time Chunking**，arXiv: [2512.05964](https://arxiv.org/abs/2512.05964)

---

## 目录



1. [问题背景与动机](#1-问题背景与动机)
2. [预备知识：动作分块与推理延迟的形式化定义](#2-预备知识动作分块与推理延迟的形式化定义)
3. [核心方法：把实时分块建模为图像修复（Inpainting）问题](#3-核心方法把实时分块建模为图像修复inpainting问题)
4. [软掩码（Soft Masking）：改进跨块连续性](#4-软掩码soft-masking改进跨块连续性)
5. [完整算法：Algorithm 1 Real-Time Chunking](#5-完整算法algorithm-1-real-time-chunking)
6. [实验设置与结果](#6-实验设置与结果)
7. [与已有工作的关系（Related Work）](#7-与已有工作的关系related-work)
8. [局限性与未来方向](#8-局限性与未来方向)
9. [后续工作：训练时版本 RTC（2025-12 论文）](#9-后续工作训练时版本-rtc2025-12-论文)
10. [对本仓库（Isaac Sim + openpi/pi0.5）部署的启示](#10-对本仓库isaac-sim--openpipi05部署的启示)
11. [参考链接](#11-参考链接)

---

## 1. 问题背景与动机

现代具身智能系统（机器人）与聊天机器人、图像生成模型的本质区别在于：**机器人始终运行在真实物理时间中**。当机器人"思考"（模型推理）时，物理世界仍在按照自身动力学演化。对语言模型而言，生成快慢只影响用户体验；而对机器人策略而言，推理延迟可能直接决定"平稳递给你一杯热咖啡"还是"洒在你腿上"。

大规模 VLA 模型（如 π0、OpenVLA、RDT 等）参数量可达数十亿，推理本身就慢，加上边缘部署常见的：

- 模型前向计算耗时（如 π0 仅 KV-cache 预填充就要 46ms，尚未开始去噪）；
- 远程/云端推理引入的网络延迟（局域网 13ms 起，条件更差可能超过 20ms）；
- 图像预处理、其它系统开销；

多重因素叠加，实际推理延迟很容易达到 100ms 甚至更高。

**动作分块（Action Chunking）**（Zhao et al., ACT 等工作提出）通过让策略一次性输出未来多个时间步的动作序列，缓解了高频控制下的时序不一致问题，已在灵巧操作任务上取得 SOTA 效果。但动作分块本身**并不能解决延迟问题**：

- 相邻两个 chunk 之间衔接处，模型可能"跳变"到与前一个 chunk 不同的策略模式（mode-jumping），产生分布外（OOD）的剧烈加速度，这类异常对基于学习的系统尤其有害，因为策略未必见过这种动力学突变。
- 简单的多预测平均（如 temporal ensembling）并不保证平均后的结果仍是合法动作，反而可能使问题恶化。

因此，论文提出核心命题：**一个好的实时系统必须产生连续、一致的控制信号，同时融合最新观测，且不打乱环境的自然动力学或模型产生正确动作的能力**。这正是 **Real-Time Chunking（RTC）** 要解决的问题——一种**推理时（inference-time）**、**无需重新训练**、可直接套用在任意扩散（diffusion）或流匹配（flow-based）VLA 上的异步执行算法。

---

## 2. 预备知识：动作分块与推理延迟的形式化定义

### 2.1 符号定义

- 策略：$\pi(\mathbf{A}_t \mid \mathbf{o}_t)$，其中 $\mathbf{A}_t = [\mathbf{a}_t, \mathbf{a}_{t+1}, \ldots, \mathbf{a}_{t+H-1}]$ 是从时刻 $t$ 起的未来动作块（chunk），$\mathbf{o}_t$ 是观测，$t$ 为控制器时间步索引。
- $H$：**预测视野（prediction horizon）**，即模型每次预测多少步动作。
- $s \le H$：**执行视野（execution horizon）**，即每个 chunk 中实际只执行前 $s$ 步就切换到下一个新 chunk。通常 $s$ 远小于 $H$ 但远大于 1（例如 $s \approx H/2$）。
  - 执行视野越长，时序一致性越好，但反应性（reactivity）越差；
  - 执行视野越短，对新信息响应越快，但更容易在 chunk 边界产生不连续的跳变。

### 2.2 流匹配（Flow Matching）动作生成

论文考虑基于**条件流匹配（conditional flow matching）**训练的策略（也可将扩散策略在推理时转换为流策略）。生成一个动作块的过程：

1. 从标准高斯分布采样初始噪声 $\mathbf{A}_t^0 \sim \mathcal{N}(0, I)$；
2. 沿着流匹配时间步 $\tau \in [0,1]$，用学习到的速度场 $\mathbf{v}_\pi$（一个神经网络）积分，去噪 $n$ 步：

$$
\mathbf{A}_t^{\tau+\frac{1}{n}} = \mathbf{A}_t^{\tau} + \frac{1}{n}\mathbf{v}_\pi(\mathbf{A}_t^{\tau}, \mathbf{o}_t, \tau) \tag{1}
$$

其中 $n$ 决定去噪步数。

### 2.3 推理延迟的定义

- $\Delta t$：控制器采样周期（如 20ms 对应 50Hz）。
- $\delta$：策略生成一个动作块所需的实际时间（wall-clock）。
- 系统被称为**实时的（real-time）**，当且仅当它保证在固定时间约束 $\Delta t$ 内对每个事件（收到 $\mathbf{o}_t$）产生响应（即 $\mathbf{a}_t$）。若 $\delta \le \Delta t$ 该约束平凡满足，但现代大模型几乎不可能做到——例如 30 亿参数的 π0 仅 KV-cache 预填充就要 46ms，已然超过 20ms 的控制周期。
- **推理延迟** $d := \lceil \delta / \Delta t \rceil$：定义为从接收观测 $\mathbf{o}_t$ 到动作块 $\mathbf{A}_t$ 可用之间，经过的控制器时间步数。
- 记 $\mathbf{a}_{t'|t}$ 为在观测 $\mathbf{o}_t$ 条件下生成的 chunk 中，第 $(t'-t)$ 个动作。若 chunk $\mathbf{A}_0$ 正在执行，执行视野为 $s$，则异步算法必须在第 $s-d$ 步就开始为下一个 chunk 启动推理。只要 $d \le H - s$，系统就能保证任何时刻都有可用动作。

### 2.4 朴素异步策略的问题（分叉现象）

朴素的异步策略是：提前 $d$ 步启动下一个 chunk 的推理，但**完全不参考**正在执行的旧 chunk 生成新 chunk。问题在于：策略无法预知在 $s-d$ 到 $s$ 之间实际会发生什么，因此 $\mathbf{a}_{s-1|0}$（旧 chunk 末尾）到 $\mathbf{a}_{s|s-d}$（新 chunk 开头）之间的过渡可能是任意的、分布外的跳变（见论文 Figure 2 的"分叉"示意）。

论文举了一个具体例子：旧 chunk 计划让机器人手臂从障碍物**上方**绕过，但因为观测/随机性差异，新 chunk 却计划从**下方**绕过——在过渡点二者被强行拼接，会导致极高的、分布外的加速度。

**Temporal Ensembling（TE，Zhao et al. 提出）**：通过对多个 chunk 里对应同一时间步的预测做加权平均来平滑过渡。论文指出该方法有根本缺陷：多个"合法"动作的平均值未必仍是"合法"动作（例如上方绕行和下方绕行的动作平均，可能直接撞上障碍物），因此延迟稍大就完全失效。

---

## 3. 核心方法：把实时分块建模为图像修复（Inpainting）问题

RTC 的核心洞见：**将实时分块问题重新表述为图像修复（inpainting）问题**。

- 新 chunk 的前 $d$ 个动作，由于生成完成时对应的时间点已经过去，**根本无法被执行**——因此直接"冻结（freeze）"为已知会被执行的、来自旧 chunk 的对应动作值。
- 剩余的、与旧 chunk 不重叠的动作，需要被"填补（inpaint）"生成，同时要与被冻结的前缀保持一致——这正类似于对一张被遮挡（masked）的图像做修复。

这一思路借鉴了扩散/流匹配模型天然适合做 inpainting 的特性（即使模型未曾针对 inpainting 专门训练过），并采用 Pokle et al. 提出的 **训练无关（training-free）图像修复算法**（基于伪逆引导 ΠGDM, Pseudoinverse-Guided Diffusion Models）加以改造。

### 3.1 基于流匹配的推理时 Inpainting

设 $\mathbf{Y}$ 为期望结果的（部分已知、部分被遮挡的）目标，$\mathbf{W}$ 为掩码（mask）。ΠGDM 在每一步去噪时，给学习到的速度场 $\mathbf{v}$ 添加一个基于梯度的引导项，鼓励最终生成结果与 $\mathbf{Y}$ 在未遮挡区域一致：

$$
\mathbf{v}_{\Pi GDM}(\mathbf{A}_t^\tau, \mathbf{o}_t, \tau) = \mathbf{v}(\mathbf{A}_t^\tau, \mathbf{o}_t, \tau) + \min\left(\beta, \frac{1-\tau}{\tau \cdot r_\tau^2}\right)\left(\mathbf{Y} - \widehat{\mathbf{A}}_t^{1}\right)^\top \mathrm{diag}(\mathbf{W}) \frac{\partial \widehat{\mathbf{A}}_t^{1}}{\partial \mathbf{A}_t^\tau} \tag{2}
$$

其中：

$$
\widehat{\mathbf{A}}_t^{1} = \mathbf{A}_t^\tau + (1-\tau)\mathbf{v}(\mathbf{A}_t^\tau, \mathbf{o}_t, \tau) \tag{3}
$$

$$
r_\tau^2 = \frac{(1-\tau)^2}{\tau^2 + (1-\tau)^2} \tag{4}
$$

- $\widehat{\mathbf{A}}_t^{1}$ 是"最终完全去噪后动作块"的**一步估计**；
- $\mathbf{W}$ 是掩码；在图像 inpainting 中，$\mathbf{Y}$ 是被遮挡的图像、结果需在非遮挡区域与 $\mathbf{Y}$ 一致；
- 引导项是一个向量-雅可比积（vector-Jacobian product），可通过反向自动微分高效计算，无需显式求雅可比矩阵。
- $\beta$ 为**引导权重裁剪（guidance weight clipping）超参数**，是本论文的新增改动之一——作者发现，若不裁剪，在控制问题常用的、去噪步数很少的场景下，算法会变得不稳定（详见附录 A.2 消融实验）。

在本文的设定中，$\mathbf{Y}$ 是"旧 chunk 剩余可用的动作序列"，$\mathbf{W}$ 用来标记哪些时间步是"已冻结、必须匹配旧 chunk"的。

---

## 4. 软掩码（Soft Masking）：改进跨块连续性

### 4.1 硬掩码的局限

朴素做法（"硬掩码" hard masking）：只把旧 chunk 的**前 $d$ 步**（已冻结、保证会被执行的部分）设为掩码值 1，其余为 0。论文发现这**在实践中往往不够**，尤其当 $d$ 较小时：ΠGDM 引导信号太弱，新 chunk 仍可能切换到不同策略模式，导致不连续（见论文 Figure 4）。

### 4.2 软掩码方案

RTC 的解决方案：不仅让前 $d$ 步硬性冻结，还让**所有 $H-s$ 个与旧 chunk 重叠的时间步**都参与跨块连续性约束，通过**软掩码（soft masking）**——即让掩码 $\mathbf{W}$ 取实数值而非仅 0/1：

- 前 $d$ 步（已冻结、必然会被执行）：权重为 **1**；
- 最后 $s$ 步（超出旧 chunk 范围，需要全新生成）：权重为 **0**；
- 中间 $d$ 到 $H-s$ 之间的区域：权重从 1 **指数衰减**到 0，体现"越靠后的未来，不确定性越大，应赋予越低约束权重"的直觉。

数学表达式：

$$
\mathbf{W}_i = \begin{cases}
1 & \text{if } i < d \\
c_i \cdot \dfrac{e^{c_i} - 1}{e - 1} & \text{if } d \le i < H - s \\
0 & \text{if } i \ge H - s
\end{cases}
\quad \text{where} \quad
c_i = \frac{H - s - i}{H - s - d + 1}, \quad i \in \{0, \ldots, H-1\} \tag{5}
$$

直观理解（对应论文 Figure 3 示意图）：

```
执行视野 s        推理延迟 d         中间区（H-d-s 个动作）        新执行视野 s
（已过去的步数）  （冻结，必然执行）  （可能被更新，指数衰减权重）    （超出旧chunk，需全新生成）
    a₋₃ a₋₂ a₋₁  |  a₀ a₁ a₂ a₃   |   a₄ ... a₁₀              |  a₁₁ a₁₂ ... a₁₅
                  guidance weight=1     指数衰减 1→0                weight=0
```

- 图中示例：若新 chunk 在 $a_{-1}$ 执行完毕后开始推理，推理延迟 $d=4$，则 $a_{0:3}$ 被"冻结"，以权重 1 参与引导；$a_{4:10}$ 是"中间区"，因推理完成时它们尚未被执行，可以被更新，但仍以指数衰减权重鼓励与旧计划一致；最后 $s=5$ 步（$a_{11:15}$）超出旧 chunk 范围，需要完全重新生成。
- 执行视野 $s$ 是一个**超参数**，需满足约束 $d \le s \le H-d$。

论文的对比实验（Figure 4）显示：硬掩码方案对"冻结区域"匹配得不够好，导致方向切换更快、更不连续；软掩码方案显著改善了跨 chunk 的连续性。

---

## 5. 完整算法：Algorithm 1 Real-Time Chunking

系统由两部分并发运行组成：**控制器接口**（`GetAction`，每 $\Delta t$ 被调用一次，消费上一时刻动作、提供最新观测）与**后台推理线程**（`InferenceLoop`，持续预测下一个 chunk），二者通过互斥锁 $\mathcal{M}$ 和条件变量 $\mathcal{C}$ 同步共享状态。

**输入**：流策略 $\pi$（预测视野 $H$）、最小执行视野 $s_{\min}$、互斥锁 $\mathcal{M}$ 及关联条件变量 $\mathcal{C}$、初始 chunk $\mathbf{A}_{\text{init}}$、初始延迟估计 $d_{\text{init}}$、延迟缓冲区大小 $b$、去噪步数 $n$、最大引导权重 $\beta$。

```
procedure InitializeSharedState
    t = 0; A_cur = A_init; o_cur = null

function GetAction(o_next):          # 由控制器以 Δt 间隔调用
    with M acquired:
        t = t + 1
        o_cur = o_next
        notify C
        return A_cur[t - 1]

procedure InferenceLoop:             # 在后台线程中循环运行
    acquire M
    Q = new Queue([d_init], maxlen=b)         # 保存最近若干次实际观测到的推理延迟
    loop:
        wait on C until t >= s_min
        s = t                                  # s = 距离上次推理启动以来，已执行的动作数
        A_prev = A_cur[s, s+1, ..., H-1]        # 去掉已经执行过的 s 步
        o = o_cur
        d = max(Q)                              # 保守地估计下一次推理延迟（取历史最大值）
        with M released:
            A_new = GuidedInference(π, o, A_prev, d, s)
        A_cur = A_new                           # 一旦新 chunk 就绪立刻切换
        t = t - s                               # 重置 t，使其重新从 0 索引 A_new
        enqueue t onto Q                        # 记录本次实际观测到的延迟

function GuidedInference(π, o, A_prev, d, s):
    用 Eq.5 计算 W；将 A_prev 右侧补齐(right-pad)到长度 H；初始化 A^0 ~ N(0, I)
    for τ = 0 to 1 步长 1/n:
        定义去噪函数 f_Â¹: A' ↦ A' + (1-τ) v_π(A', o, τ)     # 对应 Eq.3
        e = (A_prev - f_Â¹(A^τ))^T diag(W)                  # 加权误差项，对应 Eq.2
        g = e · ∂f_Â¹/∂A' |_{A'=A^τ}                         # 通过自动微分计算向量-雅可比积
        A^{τ+1/n} = A^τ + (1/n)(v_π(A^τ, o, τ) + min(β, (1-τ)/(τ·r_τ²)) · g)   # 对应 Eq.1
    return A^1
```

**关键设计点**：

1. **延迟的保守估计**：`InferenceLoop` 用队列 $Q$ 记录最近 $b$ 次实际观测到的推理延迟，取**最大值**作为下一次的延迟估计 $d$——这是保守策略，宁可多冻结一些步骤，也不要因低估延迟导致执行断档。
2. 执行视野 $s$ **可以逐 chunk 变化**（取决于本次实际执行了多少步才轮到新 chunk 就绪），实际视野为 $\max(d, s_{\min})$。
3. 整个引导推理过程只需在已训练好的流策略基础上，于**推理时**加一层梯度引导，不需要改变模型架构或训练流程。

---

## 6. 实验设置与结果

论文围绕三个问题设计实验：(1) RTC 相较现有方法，在高动态/随机环境及不同推理延迟下表现如何？(2) 软掩码对 RTC 有多重要？(3) RTC 对真实世界灵巧机器人的性能与速度分别有何影响？

### 6.1 仿真基准：Kinetix（新提出的 12 任务动态基准）

- 现有模仿学习基准大多是**准静态的**，标准分块执行（配合足够长执行视野）已可接近满分成功率，无法体现推理延迟的影响。作者因此在 **Kinetix** 模拟器（基于力控制）中构建了一个新基准：**12 个高动态、随机环境**，其中 10 个取自已有环境，2 个是新设计的（涉及投掷、接住、平衡等动态动作）。该模拟器没有"保持位置"的概念，因此推理延迟必然要求异步执行。
- 为增加真实性，动作中加入高斯噪声模拟不完美执行，闭环修正因此变得至关重要。
- **数据生成**：先用 RPO 训练专家策略（二值成功奖励），每个环境 6 个不同随机种子的专家，生成 100 万条转移数据。
- **策略训练**：训练预测视野 $H=8$ 的动作分块流策略，采用 4 层 MLP-Mixer 架构，训练 32 个 epoch。
- **评测**：报告基于 2048 次 rollout 的二值成功率；模拟延迟范围为 0（完全闭环）到 4（$H=8$ 时支持的最大延迟）。

**对比基线**：

- **Naive async**：生成新 chunk 时完全不参考旧 chunk，直接朴素切换；
- **Bidirectional Decoding (BID)**：通过拒绝采样在 chunk 之间保持连续性（批大小 32，模式数 $K=3$，用训练 8 epoch 的 checkpoint 作为弱策略）；
- **Temporal Ensembling (TE, Zhao et al.)**：维护一个预测 chunk 的缓冲区，对同一时间步的多个预测做加权平均。

**结果（Figure 5）**：

- TE 全面表现不佳，即使延迟 $d=0$ 时也是如此——印证了论文观点：对多个合法动作取平均，得到的结果未必仍是合法动作。
- RTC 对推理延迟的鲁棒性最强，随延迟增大，与 BID 的差距进一步拉大。
- BID 需要显著更多的计算量（论文附录 A.3 测得，在与 π0.5、批大小 16 的设置下，BID 延迟是 RTC 的 **2.3 倍**）。
- 硬掩码明显不如软掩码，尤其在延迟 $d$ 较小时差距更明显，验证了第 4 节软掩码方案的必要性。
- 在执行视野对比中，RTC 得益于跨 chunk 的连续性，**执行视野越短、闭环修正越及时，表现越好**——而其他方法在执行视野缩短时未必能利用好这一优势。

### 6.2 真实世界实验

**基础模型**：π0.5 VLA（Physical Intelligence 自研 VLA，双 6 自由度机械臂 + 平行夹爪的双臂系统）。

**设置**：

- $H=50$（预测视野，约合 1 秒动作），$\Delta t = 20\text{ms}$（50Hz 控制频率），去噪步数 $n=5$。
- 本地模型延迟约 76ms（作为基线的同步推理设置）；引入局域网远程推理会额外增加 10-20ms 网络延迟，使 RTC 的实际延迟约为 97ms，对应推理延迟 $d \approx 6$。
- 为模拟更大模型/更差网络/云端推理场景，额外测试 **+100ms** 和 **+200ms** 的人工注入延迟，分别对应 $d \approx 11$ 和 $d \approx 16$。

**6 项真实任务**（每任务 10 次试验，共 480 episodes，累计约 28 小时纯机器人执行时间）：

| 任务                                                          | 子步数 | 超时上限 |
| ------------------------------------------------------------- | ------ | -------- |
| Light candle（点蜡烛：拿起火柴划燃、点蜡烛、丢入碗中）        | 5      | 40s      |
| Plug ethernet（插网线：拿起网线一端，插入机架，另一端重复）   | 6      | 120s     |
| Make bed, mobile（移动叠被：将被角、两个枕头从床脚移到床头）  | 3      | 200s     |
| Shirt folding（叠衬衫：从摊平状态折叠）                       | 1      | 300s     |
| Batch folding（批量叠衣：从箱中取出多样衣物摊平、折叠、码放） | 4      | 300s     |
| Dishes in sink（放碗碟：把 4 个不同物品从台面移入水槽）       | 8      | 200s     |

**基线对比**：

- **Synchronous**（同步）：执行 $s=25$ 步后暂停等待下一 chunk，是现有工作的默认策略；
- **TE, sparse**：类似 naive async，但并行计算下一 chunk 时也做 TE 平滑；
- **TE, dense**：尽可能频繁地做推理（$s=d$），确保始终有至少 2 个重叠 chunk 参与 ensembling。
- 未与 BID 直接对比真实机器人（因仿真结果已证明 BID 计算量过大且性能更差）。

**结果（Figure 6）**：

- 用"平均任务吞吐量"（完成比例 / episode 时长）这一综合速度+性能的指标衡量：**RTC 在所有延迟条件下都取得最佳分数**，在 +100ms、+200ms 时的优势具有统计显著性。
- **RTC 对注入延迟完全鲁棒**，吞吐量几乎不随延迟增加而下降；而同步推理的吞吐量随延迟线性下降；两种 TE 变体在 +100ms、+200ms 下**完全无法运行**——由于震荡过于剧烈，触发了机器人的保护性停止。
- 剔除推理暂停时间后（用"控制器步数"而非"真实时间"衡量进度），RTC 仍然比同步推理**更快完成任务**——说明 RTC 带来的不只是"减少等待时间"，还**减少了执行错误/重试次数**。
- 在 **Light candle**（精度要求最高、且是唯一不支持重试的任务）中，RTC 相较同步推理有明显优势，体现在最终成功率上；**Bed making** 是整体难度最大的任务（策略难以准确操作枕头），RTC 在此任务上效果也最突出。
- 论文强调：即便在人工注入 300ms 以上延迟（超过模型预测视野的 30%）时，RTC 仍能完成如点蜡烛这样高精度任务。

---

## 7. 与已有工作的关系（Related Work）

- **动作分块 / VLA / 级联控制**：动作分块受人类运动控制理论启发，已成为模仿学习事实标准，常与底层高频控制器（如 PID）组成级联控制（cascade control）——但本文的仿真实验中策略直接输出力矩/力，并非这种级联设置；作者将学习式策略与级联控制理论结合的探索留给未来工作。
- **降低推理延迟的其它路线**：包括蒸馏出更高效的迭代去噪策略（Consistency Policy）、精简去噪步数的训练配方（Streaming Diffusion Policy）、并行解码（Kim et al. 对 OpenVLA 的改造）等——这些方法致力于**减少单次前向的耗时**，与 RTC（着眼于**在给定耗时约束下如何异步、连续地执行**）是正交、互补的方向。只要前向计算时间仍长于控制周期，就仍然需要类似 RTC 的实时执行机制。
- **Inpainting 与引导（Guidance）**：预训练扩散/流模型做图像 inpainting 已有丰富文献；本文借鉴其中一支（Pokle et al.），并新增软掩码和引导权重裁剪两项改造以适配控制场景。序列决策领域的 Diffuser 也用 inpainting 做长程规划，但其 inpainting 不基于 guidance；本文是**首个将 inpainting 或 guidance 应用于"实时执行"场景**的工作。
- **实时控制历史**：与模型预测控制（MPC）思路类似——都基于滚动时间窗口（receding horizon）规划，并行化执行与计算，用旧计划暖启动（warm-start）新一轮规划。但传统 MPC 依赖显式、手工设计的动力学模型与代价函数，不适用于本文考虑的、面向非结构化开放世界任务的无模型模仿学习策略。
- **Bidirectional Decoding (BID)**：最相关的先前工作，通过拒绝采样实现闭环执行，但未考虑推理延迟问题；本文的引导式 inpainting 思路可实现同样的连续性效果。仿真对比显示 BID 性能不及 RTC，且计算量显著更大（延迟是 RTC 的 2.3 倍）。
- **System 1/2 分层 VLA 架构**（如 π0.5+ 后续的分层设计）：将模型拆分为高层规划（System 2，慢、大）和低层动作生成（System 1，快、轻量），是与 RTC **正交**的方向——限制 System 1 组件规模能降低延迟，但需要单独的训练配方。

---

## 8. 局限性与未来方向

论文明确指出的局限：

1. **计算开销**：相比直接从底层策略采样，RTC 引入了显著的额外计算开销（引导项需要梯度计算与向量-雅可比积），这是它与"直接采样"方法相比的代价。
2. **仅适用于扩散/流式策略**：RTC 的方法论建立在迭代去噪（扩散/流匹配）框架之上，无法直接用于其它类型的策略架构（如自回归离散化动作 tokenizer）。
3. **真实世界实验的任务覆盖面**：虽然涵盖了多种富有挑战性的操作任务，但还有更多可能从实时执行中显著获益的**高动态场景**未被覆盖——例如**腿式运动（legged locomotion）**，该场景只出现在仿真基准中而未在真实世界实验中评测。
4. 博客原文脚注中还提及：早期非正式版本的 RTC 曾用于制作部分演示视频，但未纳入定量评测结果——提醒读者演示视频效果与论文基准测试结果之间存在版本差异。

未来方向（论文 Discussion 部分展望）：

- 需要在**多个抽象层级、多个时间尺度**上进行推理（呼应 System1/2 分层设计的思路）；
- 复杂动态运动规划、以及"遇到难题主动暂停、思考更久"这类自适应行为，仍是开放挑战；
- 随着模型规模进一步扩大，延迟问题会愈发严重，需要更精密的实时推理机制——这也是后续训练时版本 RTC（见第 9 节）的直接动机。

---

## 9. 后续工作：训练时版本 RTC（2025-12 论文）

**标题**：*Training-Time Action Conditioning for Efficient Real-Time Chunking*
**arXiv**：[2512.05964](https://arxiv.org/abs/2512.05964)（2025 年 12 月）

**动机**：原始 RTC 通过**推理时** ΠGDM 梯度引导（含反向自动微分求向量-雅可比积）实现 inpainting，虽然效果好，但这个引导过程**本身会增加推理延迟**——对一个本来就致力于降低延迟影响的系统而言，这是一个不小的讽刺性开销。

**核心改动**：将"生成时依赖旧 chunk 做 inpainting 引导"这一逻辑，从**推理阶段前移到训练阶段**：

- 训练时**模拟推理延迟**，让模型直接学习"以动作前缀（action prefix，即已提交、将要执行的部分动作）为条件"生成后续动作，而不是在推理时用梯度引导去逼近这个约束；
- 因此推理阶段不再需要额外的梯度计算/自动微分开销，**消除了推理时的额外计算量**；
- 该方法**不需要修改模型架构或机器人运行时**，只需"几行代码"的改动即可实现。

**结果**：

- 仿真实验：在**更高推理延迟**条件下，训练时版本 RTC 优于推理时版本 RTC；
- 真实世界实验：基于 **π*0.6**（下一代 VLA）在"搭积木盒子（box building）"和"制作浓缩咖啡（espresso making）"两项任务上，训练时 RTC 在**保持与推理时 RTC 相当的任务性能与速度**的同时，**计算成本更低**。
- 定位：训练时版本被作者定位为推理时 inpainting 方案的一个**可直接替换（drop-in replacement）**、性能持平但更省计算的实践方案，是 π*0.6 "煮咖啡"演示背后采用的实时执行机制。

---

## 10. 对本仓库（Isaac Sim + openpi/pi0.5）部署的启示

结合本仓库 `bridge/isaac_lab_pi05_eval.py` 目前采用的"固定步数重规划"（每 `--replan_steps` 步重新请求一次新动作块）策略，RTC 论文的结论有以下几点直接参考价值：

1. **当前桥接脚本的朴素重规划策略，本质上更接近论文中的"同步执行（Synchronous）"或"Naive async"基线**——即执行完固定步数后再等待/切换新 chunk，边界处可能出现论文所述的"分叉"跳变。若要进一步提升真实/仿真闭环评测的平滑度和成功率，可考虑参考 RTC 思路，在请求新 chunk 时把"仍在生效、尚未执行完的旧动作"作为软掩码约束传给策略服务端（需要 openpi 服务端支持类似 inpainting 的生成接口，目前 `serve_policy.py` 默认接口不直接支持）。
2. **延迟隔离的必要性**（本仓库已采用）：论文和本仓库实践都强调"策略推理"与"仿真/控制"应尽量物理隔离（不同 GPU/进程），因为推理延迟本身不可避免，关键在于系统如何**优雅地应对**这一延迟，而不是假设延迟为零。
3. **执行视野 $s$ 的选择是一个需要权衡的超参数**：本仓库 `--replan_steps` 参数即对应论文中的执行视野 $s$。若网络+推理延迟较大（例如共享 GPU、显存竞争导致的 JAX 抢占开销），适当增大 `--replan_steps`（更长执行视野）能提高稳定性但降低反应性；若追求高精度任务（类似 light candle），应考虑更短执行视野配合类 RTC 的连续性约束，而非单纯依赖同步等待。
4. 若未来希望进一步减小推理时开销并借鉴训练时版本 RTC 的思路，需要在 **openpi 微调阶段**引入"模拟延迟 + 条件化于动作前缀"的训练数据增强，这需要修改 `openpi` 的训练配置（`compute_norm_stats.py` / `train.py` 对应的 `DataConfig`），而非仅在 `bridge` 侧做推理时改造。

---

## 11. 参考链接

- 论文（NeurIPS 2025，含完整公式与算法）：https://arxiv.org/abs/2506.07339
- 项目主页/博客（含实测视频与直观图示）：https://pi.website/research/real_time_chunking
- 训练时改进版后续论文（2025-12）：https://arxiv.org/abs/2512.05964
- 相关工作：
  - π0（VLA 基础模型）：arXiv:2410.24164
  - Bidirectional Decoding (BID)：Liu et al.
  - Action Chunking with Transformers (ACT) / Temporal Ensembling：Zhao et al.
  - Diffuser（序列决策的 inpainting 式规划）
